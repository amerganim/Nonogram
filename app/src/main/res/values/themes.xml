<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose draws everything; this exists only so the window has a sane background
      before the first frame. Phase 4 matches these to the real theme so launch does
      not flash the wrong colour.
    -->
    <style name="Theme.DailyNonogram" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
